@model HomeViewModel

@{
    ViewBag.PageTitle = Model.PageTitle;
    ViewBag.MetaDescription = Model.MetaDescription;
    ViewBag.CanonicalUrl = Model.CanonicalUrl;
}

<!-- Hero Section -->
<section
    class="relative section-padding bg-gradient-to-br from-[var(--color-primary)] via-[var(--color-secondary)] to-[var(--color-accent)] text-white overflow-hidden">
    <!-- Abstract Background Pattern -->
    <div
        class="absolute inset-0 opacity-10 bg-[radial-gradient(ellipse_at_top,_var(--tw-gradient-stops))] from-white via-transparent to-transparent">
    </div>
    <div class="absolute inset-0 opacity-5"
        style="background-image: url('data:image/svg+xml,%3Csvg width=\'60\' height=\'60\' viewBox=\'0 0 60 60\' xmlns=\'http://www.w3.org/2000/svg\'%3E%3Cg fill=\'none\' fill-rule=\'evenodd\'%3E%3Cg fill=\'%23ffffff\' fill-opacity=\'1\'%3E%3Cpath d=\'M36 34v-4h-2v4h-4v2h4v4h2v-4h4v-2h-4zm0-30V0h-2v4h-4v2h4v4h2V6h4V4h-4zM6 34v-4H4v4H0v2h4v4h2v-4h4v-2H6zM6 4V0H4v4H0v2h4v4h2V6h4V4H6z\'/%3E%3C/g%3E%3C/g%3E%3C/svg%3E');">
    </div>

    <div class="container-custom relative z-10">
        <div class="max-w-4xl mx-auto text-center fade-in space-y-8">
            <div
                class="inline-flex items-center gap-2 px-4 py-1.5 rounded-full bg-white/10 backdrop-blur-md border border-white/20 text-sm font-medium shadow-lg hover:bg-white/20 transition-colors cursor-default">
                <span class="flex h-2 w-2 relative">
                    <span
                        class="animate-ping absolute inline-flex h-full w-full rounded-full bg-[var(--color-success)] opacity-75"></span>
                    <span class="relative inline-flex rounded-full h-2 w-2 bg-[var(--color-success)]"></span>
                </span>
                New Tools Available v2.0
            </div>

            <h1 class="text-6xl md:text-7xl font-extrabold tracking-tight leading-tight drop-shadow-sm">
                Welcome to <br />
                <span
                    class="text-transparent bg-clip-text bg-gradient-to-r from-white to-white/90 filter drop-shadow">NovaTools
                    Hub</span>
            </h1>

            <p class="text-xl md:text-2xl text-white/90 max-w-2xl mx-auto leading-relaxed">
                Your swift, secure, and professional suite of developer tools and calculators.
            </p>

            <div class="flex flex-wrap gap-4 justify-center pt-4">
                <a href="#tools"
                    class="btn bg-white text-[var(--color-primary)] hover:bg-blue-50 hover:scale-105 hover:shadow-xl transition-all duration-300 font-semibold px-8 py-4 rounded-xl">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5"
                            d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z"></path>
                    </svg>
                    Explore Tools
                </a>
                <a asp-controller="Blog" asp-action="Index"
                    class="btn border-2 border-white/30 bg-white/5 text-white hover:bg-white/10 hover:border-white hover:shadow-lg backdrop-blur-sm transition-all duration-300 px-8 py-4 rounded-xl">
                    <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                            d="M12 6.253v13m0-13C10.832 5.477 9.246 5 7.5 5S4.168 5.477 3 6.253v13C4.168 18.477 5.754 18 7.5 18s3.332.477 4.5 1.253m0-13C13.168 5.477 14.754 5 16.5 5c1.747 0 3.332.477 4.5 1.253v13C19.832 18.477 18.247 18 16.5 18c-1.746 0-3.332.477-4.5 1.253">
                        </path>
                    </svg>
                    Read Blog
                </a>
            </div>
        </div>
    </div>
</section>

<!-- Featured Tools -->
<section class="section-padding bg-[var(--color-bg)]" id="tools">
    <div class="container-custom">
        <div class="text-center mb-16">
            <h2 class="text-4xl md:text-5xl font-bold mb-4 tracking-tight">Featured Tools</h2>
            <p class="text-xl text-[var(--color-text-medium)]">Most popular, production-ready utilities</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
            @foreach (var tool in Model.FeaturedTools)
            {
                <a href="@tool.Url"
                    class="tool-card group no-underline relative overflow-hidden p-8 rounded-2xl bg-[var(--color-surface)] border border-[var(--color-border)] hover:border-[var(--color-primary)] transition-all duration-300 hover:shadow-2xl hover:-translate-y-1">
                    <div class="absolute top-0 right-0 p-4 opacity-10 group-hover:opacity-20 transition-opacity">
                        <svg class="w-24 h-24 text-[var(--color-primary)] transform rotate-12" fill="currentColor"
                            viewBox="0 0 24 24">
                            <path
                                d="M12 2L2 7l10 5 10-5-10-5zm0 9l2.5-1.25L12 8.5l-2.5 1.25L12 11zm0 2.5l-5-2.5-5 2.5L12 22l10-8.5-5-2.5-5 2.5z" />
                        </svg>
                    </div>

                    <div
                        class="tool-icon w-16 h-16 rounded-2xl bg-gradient-to-br from-[var(--color-primary)] to-[var(--color-secondary)] text-white text-3xl flex items-center justify-center mb-6 shadow-lg group-hover:scale-110 transition-transform duration-300">
                        @tool.Icon
                    </div>

                    <h3
                        class="text-2xl font-bold mb-3 text-[var(--color-text-dark)] group-hover:text-[var(--color-primary)] transition-colors">
                        @tool.Name</h3>
                    <p class="text-[var(--color-text-medium)] mb-6 leading-relaxed">@tool.Description</p>

                    <div class="flex items-center text-[var(--color-primary)] font-bold text-sm uppercase tracking-wide">
                        Launch Tool
                        <svg class="w-5 h-5 ml-2 group-hover:translate-x-2 transition-transform duration-300" fill="none"
                            stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5"
                                d="M17 8l4 4m0 0l-4 4m4-4H3"></path>
                        </svg>
                    </div>
                </a>
            }
        </div>
    </div>
</section>

<!-- Tool Categories -->
<section class="section-padding bg-white" id="categories">
    <div class="container-custom">
        <div class="text-center mb-16">
            <h2 class="text-4xl font-bold mb-4">Browse by Category</h2>
            <p class="text-xl text-[var(--color-text-medium)]">Everything you need, organized.</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
            @foreach (var category in Model.ToolCategories)
            {
                <div
                    class="card card-hover p-6 rounded-2xl border border-[var(--color-border)] hover:border-[var(--color-primary)] hover:shadow-xl transition-all duration-300 group bg-[var(--color-bg)]">
                    <div class="flex items-start gap-5">
                        <div
                            class="text-5xl bg-white p-3 rounded-xl shadow-sm group-hover:scale-110 transition-transform duration-300">
                            @category.Icon</div>
                        <div class="flex-1">
                            <h3 class="text-2xl font-bold mb-2 text-[var(--color-text-dark)]">@category.Name</h3>
                            <p class="text-[var(--color-text-medium)] mb-4 text-sm leading-relaxed">@category.Description
                            </p>
                            <div class="flex items-center justify-between pt-2 border-t border-[var(--color-border)]/50">
                                <span
                                    class="text-xs font-semibold text-[var(--color-text-light)] uppercase tracking-wider bg-white px-2 py-1 rounded">@category.ToolCount
                                    tools</span>
                                <a asp-controller="Home" asp-action="AllTools" asp-fragment="@category.Slug"
                                    class="text-[var(--color-primary)] font-semibold hover:text-[var(--color-primary-hover)] flex items-center text-sm">
                                    Explore <span class="ml-1 group-hover:translate-x-1 transition-transform">→</span>
                                </a>
                            </div>
                        </div>
                    </div>
                </div>
            }
        </div>
    </div>
</section>

<!-- Features Section -->
<section class="section-padding bg-[var(--color-bg)] relative overflow-hidden">
    <!-- Decor element -->
    <div class="absolute -top-24 -right-24 w-96 h-96 bg-[var(--color-primary)]/5 rounded-full blur-3xl"></div>
    <div class="absolute -bottom-24 -left-24 w-96 h-96 bg-[var(--color-secondary)]/5 rounded-full blur-3xl"></div>

    <div class="container-custom relative z-10">
        <div class="text-center mb-16">
            <h2 class="text-4xl font-bold mb-4">Why NovaTools Hub?</h2>
            <p class="text-xl text-[var(--color-text-medium)]">Engineered for performance and usability</p>
        </div>

        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6">
            <div
                class="card p-8 rounded-2xl text-center hover:shadow-xl transition-all duration-300 hover:-translate-y-2 border-t-4 border-[var(--color-primary)] bg-white">
                <div
                    class="w-16 h-16 bg-blue-50 rounded-2xl flex items-center justify-center text-3xl mx-auto mb-6 text-[var(--color-primary)] shadow-inner">
                    ⚡
                </div>
                <h3 class="text-xl font-bold mb-3">Lightning Fast</h3>
                <p class="text-[var(--color-text-medium)] text-sm leading-relaxed">Optimized algorithms ensure instant
                    calculations and results without lag.</p>
            </div>

            <div
                class="card p-8 rounded-2xl text-center hover:shadow-xl transition-all duration-300 hover:-translate-y-2 border-t-4 border-[var(--color-secondary)] bg-white">
                <div
                    class="w-16 h-16 bg-teal-50 rounded-2xl flex items-center justify-center text-3xl mx-auto mb-6 text-[var(--color-secondary)] shadow-inner">
                    📱
                </div>
                <h3 class="text-xl font-bold mb-3">Mobile First</h3>
                <p class="text-[var(--color-text-medium)] text-sm leading-relaxed">Responsive design that adapts
                    perfectly to phones, tablets, and desktops.</p>
            </div>

            <div
                class="card p-8 rounded-2xl text-center hover:shadow-xl transition-all duration-300 hover:-translate-y-2 border-t-4 border-[var(--color-accent)] bg-white">
                <div
                    class="w-16 h-16 bg-purple-50 rounded-2xl flex items-center justify-center text-3xl mx-auto mb-6 text-[var(--color-accent)] shadow-inner">
                    🔒
                </div>
                <h3 class="text-xl font-bold mb-3">Secure & Private</h3>
                <p class="text-[var(--color-text-medium)] text-sm leading-relaxed">All processing happens locally. Your
                    data never leaves your browser.</p>
            </div>

            <div
                class="card p-8 rounded-2xl text-center hover:shadow-xl transition-all duration-300 hover:-translate-y-2 border-t-4 border-yellow-400 bg-white">
                <div
                    class="w-16 h-16 bg-yellow-50 rounded-2xl flex items-center justify-center text-3xl mx-auto mb-6 text-yellow-500 shadow-inner">
                    💯
                </div>
                <h3 class="text-xl font-bold mb-3">Always Free</h3>
                <p class="text-[var(--color-text-medium)] text-sm leading-relaxed">Professional grade tools available to
                    everyone, completely free of charge.</p>
            </div>
        </div>
    </div>
</section>

<!-- CTA Section -->
<section class="section-padding bg-gradient-to-br from-[var(--color-primary)] via-[var(--color-secondary)] to-[var(--color-accent)] text-white">
    <div class="container-custom">
        <div class="max-w-4xl mx-auto">
            <div
                class="relative overflow-hidden rounded-3xl border border-white/15 bg-white/5 backdrop-blur-xl px-8 py-10 md:px-10 md:py-12 shadow-2xl">
                <div class="absolute -right-20 -top-20 w-56 h-56 bg-white/10 rounded-full blur-3xl"></div>
                <div class="absolute -left-24 -bottom-24 w-72 h-72 bg-[var(--color-primary)]/25 rounded-full blur-3xl"></div>

                <div class="relative z-10 text-center space-y-6">
                    <p class="text-xs font-semibold tracking-[0.3em] uppercase text-white/70">
                        Ready to get started?
                    </p>
                    <h2 class="text-3xl md:text-4xl font-bold">
                        Turn scattered tools into one focused workspace.
                    </h2>
                    <p class="text-lg md:text-xl text-white/85 max-w-2xl mx-auto">
                        Join thousands of users who trust NovaTools Hub for daily calculations, developer utilities,
                        and smarter workflows.
                    </p>

                    <div class="flex flex-wrap gap-4 justify-center pt-2">
                        <a href="#tools"
                           class="inline-flex items-center gap-2 px-8 py-3 rounded-full bg-white text-[var(--color-primary)] font-semibold shadow-lg shadow-black/10 hover:shadow-2xl hover:-translate-y-0.5 hover:bg-blue-50 transition-all duration-300">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5"
                                      d="M5 12h14M13 5l7 7-7 7"></path>
                            </svg>
                            <span>Browse all tools</span>
                        </a>

                        <a asp-controller="Home" asp-action="About"
                           class="inline-flex items-center gap-2 px-8 py-3 rounded-full border border-white/60 text-white/90 hover:bg-white/10 hover:text-white hover:-translate-y-0.5 transition-all duration-300">
                            <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2"
                                      d="M13 16h-1v-4h-1m1-4h.01M12 2a10 10 0 100 20 10 10 0 000-20z"></path>
                            </svg>
                            <span>Learn more about NovaTools</span>
                        </a>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

@section Scripts {
    <script>
        // Smooth scroll for anchor links
        document.querySelectorAll('a[href^="#"]').forEach(anchor => {
            anchor.addEventListener('click', function (e) {
                e.preventDefault();
                const target = document.querySelector(this.getAttribute('href'));
                if (target) {
                    target.scrollIntoView({ behavior: 'smooth', block: 'start' });
                }
            });
        });

        // Add animation on scroll
        const observerOptions = {
            threshold: 0.1,
            rootMargin: '0px 0px -100px 0px'
        };

        const observer = new IntersectionObserver((entries) => {
            entries.forEach(entry => {
                if (entry.isIntersecting) {
                    entry.target.classList.add('fade-in');
                }
            });
        }, observerOptions);

        document.querySelectorAll('.tool-card, .card').forEach(el => {
            observer.observe(el);
        });
    </script>
}